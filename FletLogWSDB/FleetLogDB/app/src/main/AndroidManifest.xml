<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- REQUISITO RÚBRICA: Permiso de internet para HttpURLConnection -->
    <uses-permission android:name="android.permission.INTERNET" />



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FleetLogDB"
        android:usesCleartextTraffic="true">

        <!-- LOGIN: Pantalla de entrada (LAUNCHER) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MAIN: Lista de Vehículos -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Conductores -->
        <activity
            android:name=".DriversActivity"
            android:exported="false" />

        <!-- Mantenimientos -->
        <activity
            android:name=".MaintenanceActivity"
            android:exported="false" />

        <!-- Combustible -->
        <activity
            android:name=".FuelActivity"
            android:exported="false" />

        <!--
            AuthCallbackActivity: Intercepta el deep link "fleetlog://auth"
            que el backend envía de vuelta a Android tras el login con Google.
            android:exported="true" es OBLIGATORIO para que el sistema operativo
            pueda lanzarla desde el navegador externo (Chrome Custom Tab).
            android:launchMode="singleTop" evita crear instancias duplicadas.
        -->
        <activity
            android:name=".AuthCallbackActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Scheme y host que coinciden con fleetlog://auth -->
                <data
                    android:scheme="fleetlog"
                    android:host="auth" />
            </intent-filter>
        </activity>

    </application>

</manifest>